let addRoomImageFiles = [];

export const getAddRoomImageFiles = () => addRoomImageFiles;

export const setAddRoomImageFiles = (files) => {
  addRoomImageFiles = files;
};

export const clearAddRoomImageFiles = () => {
  addRoomImageFiles = [];
};
